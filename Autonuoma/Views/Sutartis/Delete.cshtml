@model Org.Ktu.Isk.P175B602.Autonuoma.ViewModels.SutartisListVM
@{
	ViewData["title"] = "Sutarties šalinimas";
}

<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Index")" class="link-primary"><i class="fa-solid fa-angles-left"></i> Atgal į sarašą</a>
	</span>
</div>

<div class="container-fluid d-flex justify-content-center">
@using (Html.BeginForm()) 
{
	@Html.AntiForgeryToken()

	<fieldset class="border rounded shadow p-2">
		<legend class="text-center">Ar tikrai norite šalinti šią sutartį?</legend>

		<dl>
			<dt>@Html.DisplayNameFor(m => m.ID)</dt>
			<dd>@Html.DisplayFor(m => m.ID)</dd>

			<dt>@Html.DisplayNameFor(m => m.Data)</dt>
			<dd>@Html.DisplayFor(m => m.Data)</dd>

			<dt>@Html.DisplayNameFor(m => m.Kaina)</dt>
			<dd>@Html.DisplayFor(m => m.Kaina)</dd>

			<dt>@Html.DisplayNameFor(m => m.Busena)</dt>
			<dd>@Html.DisplayFor(m => m.Busena)</dd>

			<dt>@Html.DisplayNameFor(m => m.FkDarbuotojas)</dt>
			<dd>@Html.DisplayFor(m => m.FkDarbuotojas)</dd>

			<dt>@Html.DisplayNameFor(m => m.FkKlientas)</dt>
			<dd>@Html.DisplayFor(m => m.FkKlientas)</dd>

			<dt>@Html.DisplayNameFor(m => m.FkGyvunas)</dt>
			<dd>@Html.DisplayFor(m => m.FkGyvunas)</dd>
		</dl>

		@if( ViewData["deletionNotPermitted"] != null )
		{
			<div class="alert alert-warning text-center">
				<i class="fa-solid fa-circle-exclamation"></i> Šios sutarties esybės trinti negalima, nes 
				ji turi ryšių į priklausomas esybes.
			</div>
		}
		else
		{
			<div class="container-fluid d-flex d-row justify-content-end">
				<button
					formaction="@Url.Action("DeleteConfirm", new {id = Model.ID})"
					class="btn btn-danger"
					style="white-space: nowrap;"
				><i class="fa-solid fa-trash"></i> Šalinti</button>
			</div>			
		}
	</fieldset>
}
</div>

<div class="main-content-footer container-fluid d-flex d-row justify-content-between align-items-center">
	<span class="title"></span>
	<span class="actions">
		<a href="@Url.Action("Index")" class="link-primary"><i class="fa-solid fa-angles-left"></i> Atgal į sarašą</a>
	</span>
</div>